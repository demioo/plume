query Posts($limit: Int!, $cursor: String) {
  posts(cursor: $cursor, limit: $limit) {
    hasMore
    posts {
      id
      title
      text
      points
      creatorId
      createdAt
      updatedAt
      textSnippet
      creator {
        id
        username
      }
    }
  }
}
