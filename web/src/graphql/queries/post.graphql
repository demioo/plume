query Post($id: Int!) {
  post(id: $id) {
    id
    title
    text
    points
    creatorId
    createdAt
    updatedAt
    text
    points
    voteStatus
    creator {
      id
      username
    }
  }
}
