mutation CreatePost($input: PostInput!) {
  createPost(input: $input) {
    post {
      id
      title
      text
      points
      creatorId
      createdAt
      updatedAt
    }

    errors {
      ...BasicError
    }
  }
}
